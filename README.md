#  Frontend teste Tecnico SolutionTI

## UI

Toda parte de design do produto foi realizada via vibe code com o Gemini, ainda há algumas questões referente a cor de textos e etc, porém não foquei nisso por conta que acredito que o foco seja o CRUD.

## Axios + React Query

Foi realizado a utilização do React Query + Axios na atuação com o backend, código este que foi gerado com o auxilio de IA.
